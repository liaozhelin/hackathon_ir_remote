FUNC void displayvalues(void) {
  int idx;
 
  exec("log > MyValues.log");
  for (idx = 0; idx < 400; idx++) {
    printf ("%u,", BitValue[idx]);
  }
	printf ("\r\n");
	for (idx = 0; idx < 400; idx++) {
    printf ("%u,", KeepTime[idx]);
  }
  exec("log off");
}